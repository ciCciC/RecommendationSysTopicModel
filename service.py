

def add_like(user_id, tv_program_id):
    print(user_id, tv_program_id)


def remove_like(user_id, tv_program_id):
    print(user_id, tv_program_id)


def add_log(user_id, action_type):
    print(user_id, action_type)